# 未修改的问题

记录已评估但尚未实施的问题，供后续参考。

---

## needsPlayerDecision 进事件层

**问题**：`needsPlayerDecision`（需玩家决策的停点）只在 `GameOrder` 指令层生效，未持久化到 `eventLog`。读档回放时不会在曾需要决策的位置暂停，玩家错过决策点。

**现状**：
- `GameOrder.needsPlayerDecision` 由 AI 返回（`aiOrderContract.ts` prompt 已描述该字段）
- 调度器 `advance()` 遇到后停止并吐 toast
- 但 `GameOrder` 不进 `eventLog`，读档时这些停点信息丢失

**修复方向**：
1. `game.ts`: 新增 `GameEvent` 类型 `{ type: 'pauseForPlayer' }`
2. `game.ts` `applyEvent`: 处理 `pauseForPlayer`，返回 `{ ok: true, paused: true }`
3. `game.ts` `load()`: 遇到 `paused` 事件时中断回放，暂存剩余事件，等玩家操作后继续
4. `useGameScheduler.ts`: 调度器遇到 `needsPlayerDecision` 时追加 `applyEvent({ type: 'pauseForPlayer' })` 写入日志
5. `game.ts`: 新增"继续回放"函数，玩家决策完成后回放剩余事件

**改动量**：约 21 行，不碰 UI / executeOrder / AI prompt

**状态**：待决定是否实施（2026-07-23）

---

## 其他待评估项

### 外交消息总线（三段式）
无跨阵营对话路由，势力间无法"谈判"。电报功能已部分填补此缺口（direct 频道）。

### 规则 AI 兜底（成本收敛）
所有决策全走 LLM，无低成本规则引擎降级。

### 快照瘦身
`getSnapshot()` 返回全量 380 城，大后期 token 开销大。

### 独立时间轴事件批次
世界 AI 为回合制同步生成，无异步/并行时间线。


玩家AI操作台，目前不能做的
❌ 外交（派使团、宣战、结盟）——设计方案已写好（docs/外交消息总线设计方案.md），尚未实现
✅ 生产/建设/内政指令（已实现：`recruit`/`develop`/`fortify`/`rally`，见 `gameOrders.ts`，含成本校验、`produce`/`cityStatChange`/`moraleChange` 事件写回与动画）
❌ 战斗减员（attack 事件类型在 reducer 中已定义但无指令接通，属休眠状态）
✅ 多轮对话上下文（已实现：`useAiOrchestrator.chatTurns` + `PlayerAiPanel` 取最近 5 轮注入 prompt，并落 `narrative` 到 eventLog 跨回合持久化）


# 未修改的问题

记录审查中确认存在但暂缓修改的设计/数据问题，供后续迭代参考。

## minor 政权批量上下文缺邻接/可达性信息

- **位置**：`utils/aiContext.ts` `buildMinorContext()`
- **现象**：世界 AI 批量代打次要势力时，上下文只给每个势力的城清单 + 兵力 + 全局历史，没有邻接城市列表
- **影响**：次要势力生成的进攻指令可能跨势力、地理不可达，被战略校验拦下，白烧 token
- **对比**：主势力 AI 的 `buildFactionContext` 已包含一阶邻接城市分组
- **暂缓原因**：次要势力本身行为保守（prompt 引导"保守为上，无大事按兵不动"），实际出现概率低；补齐邻接需按势力分组建邻接表，改动量略大
- **建议方案**：在 `buildMinorContext` 中为每个次要势力追加邻接城市（复用 `adjacentMap`），格式与 `buildFactionContext` 一致

## 地形语言不一致

- **位置**：`utils/aiPromptBuilder.ts` `buildBattleSummary()` vs `buildBattleSettlePrompt()`
- **现象**：战斗裁决官的输入摘要中 `terrain=MOUNTAIN`（英文），但裁决提示词用"山地/丘陵"（中文）
- **影响**：轻微，LLM 能自行对应 MOUNTAIN→山地
- **建议**：后续统一用 `TERRAIN_LABEL` 翻译
