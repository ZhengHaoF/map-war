# map-war 项目进度评估（纯代码 · 2026-08-07）

> 评估方法：应要求**不阅读任何文档**，仅基于源码、git 历史、构建/测试配置做代码级评估。
> 验证手段：实跑单元测试与类型检查（见第二节），结论有硬数据支撑。

---

## 一、总体结论

**架构层面：完成度高，可称为"功能闭环已成形"。** 一个军阀推演游戏该有的骨架——世界态、回合引擎、AI 决策、战斗、经济、外交、存档回放、后端代理——全部有实现且互相打通，不是 demo 拼凑。

**工程健康度：测试全绿，类型检查已修复通顺。** 详见第二节。

**进度比喻：骨架 95%，血肉 80%，UI 收尾 80%。** 能跑通一整局（经济 → 决策 → 战斗 → 外交 → 存档）；HUD 玩家状态面板四个占位模块中，军事力量 / 外交态势 / 事件日志已于当晚接入真实数据（见第五节），**仅剩「周边威胁」一个占位**（需邻接表数据工程）。

---

## 二、验证状态（已实跑）

| 验证项 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `vitest run`（pool 已在配置固化为 threads） | **216 passed / 14 files，全绿**（gameStore / aiOrderContract / diplomacyBus / llmClient 等均有覆盖） |
| 类型检查 | `pnpm typecheck`（vue-tsc --noEmit） | 跑前 **失败 2 处**，修复后 **exit 0 通过** |

类型错误根因：`LeafletMap.vue` 中 `useToast().push({ tone: 'success' })` 使用了 `ToastTone` 枚举中不存在的值（正确值为 `'green'`=青绿=成功）。两处已改为 `'green'`，复跑通过。**该修复已随本次提交固化。**

> **✅ vitest 环境怪病（Git Bash / Windows）—— 已修复**：`vitest run` 默认 forks pool 在 Windows + Git Bash 下会**静默退出 1 且无任何输出**（`child_process.fork()` 进程通信崩溃）。已于 2026-08-08 在 `vitest.config.ts` 固化 `pool: 'threads'`，`package.json` 的 `test:unit` 也加了 `--pool=threads` 兜底，无需再手动传参。

---

## 三、各子系统成熟度（代码层）

| 子系统 | 核心文件 | 行数/规模 | 成熟度 | 备注 |
|---|---|---|---|---|
| 世界态 Kernel | `stores/game.ts` | 1236 行 / 21 事件类型 | ✅ 完成 | reducer 模式清晰，`applyEvent` 唯一写者，含存亡审计与存档重放；另有 `myMilitary` / `myDiplomacy` 两个玩家向派生 |
| 回合编排 | `useAgentKernel.ts` | P0→P4 | ✅ 完成 | 经济 → 分类 → 并行决策 → 战斗结算 → 叙事+日期，闭环 |
| AI 指令契约 | `aiOrderContract.ts` | 17 指令 | ✅ 完成 | 结构 + 战略双层校验；god-mode / 玩家提示词齐备 |
| 指令执行 | `gameOrders.ts` | executeOrder | ✅ 完成 | 17 条 ORDER_TYPES 全部分发 |
| 战斗系统 | `battleFormula` + `battleFlavorAi` + scheduler P4a | — | ✅ 完成 | 确定性减员 + AI 调味 + 士气崩溃 + 偷家检测 |
| 双轨经济 | `treasuryChange` / `granaryChange` / `economicTick` | — | ✅ 完成 | 银库/粮仓双轨，欠饷扣士气、缺粮损兵 |
| 外交总线 | `useDiplomacyBus.ts` | 三段式 + 国家援助 | ✅ 完成 | Phase1/2/3 + 独立 `diplomacyRecords` 持久化；2026-08-08 国家援助（求援列强）落地 |
| 存档/回放 | `save/load` + eventLog replay | — | ✅ 完成 | localStorage 持久化，读档重放 |
| 后端 AI 代理 | `server/ai.service.ts`（NestJS） | — | ✅ 完成 | DeepSeek 转发 + healthCheck |
| 地图/UI | `LeafletMap.vue` 等 27 组件 | 见第四节 | 🟡 基本完成 | 局部缺口见第五节 |
| 玩家状态 HUD | `PlayerStatusPanel.vue` | 672 行 | 🟡 3/4 完成 | 军事/外交/事件日志已接真实数据（commit 0ba7121）；仅「周边威胁」占位（见第五节） |
| 游戏结束 | `GameOverModal.vue` | 新 | ✅ 已实现 | 已接入 `App.vue:9/65`，随本次提交固化 |

---

## 四、UI 体量

- **27 个 `.vue` 组件 / 65 个 `.ts` 文件 / ~11,000 行 Vue 代码**
- 体量 Top（行数）：`LeafletMap.vue` 3213 / `TelegramTab.vue` 910 / `OnboardingView.vue` 796 / `PlayerAiPanel.vue` 637 / `PlayerStatusPanel.vue` 672（三模块接入后 +145） / `SaveSelectorModal.vue` 606 / `FactionDebugPanel.vue` 536 / `EnvoyTab.vue` 482 / `AdvisorPanel.vue` 459 / `AiDebugPanel.vue` 452
- 说明：调试面板（FactionDebug / AiDebug）占近 1000 行，开发期投入重；玩家向面板（PlayerAi / Telegram / Envoy / Advisor）也相当完整。

---

## 五、明确未完成 / 在途

1. **`PlayerStatusPanel.vue` 的「周边威胁」占位** —— 全代码库**仅剩**一处「待建」标记。原 4 个占位模块（军事力量 / 外交态势 / 周边威胁 / 事件日志）中 **3 个已于当晚接入真实数据（commit `0ba7121`）**：
   - 军事力量：`myMilitary` 派生（总兵力=Σ驻军+野战、加权士气、城防、兵力榜），含驻军/野战拆分
   - 外交态势：`myDiplomacy` 派生（战/盟/和计数 + 逐势力状态、停战冷却期、对方兵力）
   - 事件日志：`EventLogPanel` 新增 `limit` prop，内嵌最近 12 条（全量仍在地图面板）
   - 三者均为 store 纯派生 computed，不碰 `applyEvent` / 存档 / replay。
   - **周边威胁的根因**：威胁=与玩家城市接壤的敌方城市及驻军，但 `chinaCities.seed.json`（380 城）**无邻接字段**。`scripts/build_adjacency.py`（shapely 几何相交）已写好但**从未运行**，`adjacent` 字段代码 0 处消费。这是全库唯一需要**数据工程**（而非接线）的模块。

2. **AI 模块新旧并存（技术债，非功能缺口）** —— `utils/ai/`（index/invoke/context/prompts）与旧 `aiInvoke` / `aiPromptBuilder` / `aiContext` 兼容层仍互相 import，似渐进迁移中。测试可通过，但两套同名概念并存，后续维护有认知负担。

3. **（用户此前暂缓，记录在案）** 旧地名、P2 工具栏分组 —— 非阻塞，先放着。

---

## 六、判断与建议（优先级）

1. **「周边威胁」邻接表数据工程** —— 当前唯一的功能断点。方案：装 shapely → 跑 `scripts/build_adjacency.py` 生成 `chinaCities.seed.adjacent.json` → 独立成 `data/cityAdjacency.ts`（`Record<gb, gb[]>`，不动种子结构）→ store 加 `myThreats` 派生（接壤敌城按驻军排序 + 敌我兵力比）→ 面板接线。工作量约半天。
2. **AI 模块去重** —— 不急，但趁热把旧 `aiInvoke/aiPromptBuilder/aiContext` 兼容层拆掉，统一到 `utils/ai/`，免得改一处漏一处。
3. **（已固化）** 在途改动均已提交：`23e5692`（游戏结束结算 + 存亡审计）、`b4058f5`（GameOverModal + 评估文档）、`0ba7121`（HUD 三模块）。

---

## 七、提交记录（2026-08-07）

当晚共 3 次提交固化全部在途改动：

**`23e5692`** feat(game): 添加游戏结束结算和势力存亡自动审计功能

```
M  apps/web/src/App.vue
M  apps/web/src/components/LeafletMap.vue   (含 ToastTone 'success'→'green' 修复)
M  apps/web/src/data/gameConfig.ts
M  apps/web/src/stores/game.ts
A  apps/web/src/components/GameOverModal.vue   (新)
```

**`b4058f5`** docs: 新增项目进度评估 + 提交游戏结束弹窗

```
A  apps/web/src/components/GameOverModal.vue   (新)
A  docs/项目进度评估-2026-08-07.md             (本文件)
```

**`0ba7121`** feat(hud): PlayerStatusPanel 军事/外交/事件日志三模块接入真实数据

```
M  apps/web/src/stores/game.ts                 (MyCityStat 补军事字段 + myMilitary/myDiplomacy 派生)
M  apps/web/src/components/EventLogPanel.vue   (新增 limit prop)
M  apps/web/src/components/PlayerStatusPanel.vue (删 4 占位，实现三模块)
M  apps/web/src/__tests__/gameStore.spec.ts    (+2 派生聚合单测，213 passed)
```

> 注：`docs/开局设定文档.md` 与 `docs/设计文档.md` 曾于 2026-08-07 上午出现在工作树 modified，但未纳入任何提交——按项目提交纪律（不把 docs 等无关未跟踪文件一并入库），留待单独提交。

---

## 八、提交记录（2026-08-08）

**`aa3e884`** feat(diplomacy): 国家援助（求援列强）完整功能落地

```
M  apps/web/src/components/EnvoyTab.vue            (遣使下拉：中国势力/列强国家分组；协商气泡绿色援助 tag)
M  apps/web/src/composables/useDiplomacyBus.ts      (country 分支：三段 + forceSettle 双向执行 + 条约落 eventLog)
M  apps/web/src/models/diplomacy.ts                 (intent 'aid' / AidOffer / targetCountry)
M  apps/web/src/stores/aiDiplomacy.ts               (国家分支：领袖人设 + 国力数据 + 玩家历史时间线 + parseAidOffer)
M  apps/web/src/stores/worldCountries.ts            (countryName / COUNTRY_OPTIONS 7 国)
M  apps/web/src/__tests__/aiDiplomacyBus.spec.ts    (+3 单测：财粮军援到账 / 偿付扣银 / 军援目标城非玩家→条约作废)
```

**`（待提交）`** fix(test): vitest forks pool 静默退出修复

```
M  apps/web/vitest.config.ts                        (pool: 'threads')
M  apps/web/package.json                            (test:unit 加 --pool=threads)
```
