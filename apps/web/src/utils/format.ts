/**
 * 数值格式化工具模块。
 *
 * 浮点精度公约：项目不做大数/金融精度，用 round1（保留一位小数）收口即可。
 * 业务尺度上所有银/粮/比例均以 0.1 为最小分辨率。
 */

/** 保留一位小数，消除 JS 浮点累加误差（如 70.80000000000001 → 70.8） */
export function round1(n: number): number {
  return Math.round(n * 10) / 10
}
